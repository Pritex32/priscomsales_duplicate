name: Deploy Streamlit App to Render

on:
  push:
    branches:
      - main  # or 'master' depending on your branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📂 Install dependencies

        run: |
          python -m pip install --upgrade pip
          pip install -r chubuzo_sales/requirements.txt

      - name: 🚀 Trigger Render Deploy
        run: |
          curl -X POST "https://api.render.com/deploy/srv-d1g010jipnbc73a4m080?key=gnVUa1xK474"
